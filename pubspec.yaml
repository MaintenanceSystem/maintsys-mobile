name: maintsys_flutter
description: MaintSys - Sistema de Manutenção Industrial 4.0
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP & API
  dio: ^5.4.0

  # Navigation
  go_router: ^13.2.0

  # Storage (token)
  flutter_secure_storage: ^9.0.0

  # UI
  fl_chart: ^0.68.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
